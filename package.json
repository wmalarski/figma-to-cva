{
  "name": "figma-to-cva",
  "version": "1.0.0",
  "description": "Your Figma Plugin",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "biome lint --write ./apps ./shared",
    "format": "biome check --write ./apps ./shared && pnpm biome format --write ./apps ./shared"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "turbo": "^2.4.4"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.6.2",
  "pnpm": {
    "onlyBuiltDependencies": ["@biomejs/biome", "esbuild"]
  }
}
